// Database service - now using Supabase adapter
export { getDB, type TDB } from './db-adapter';

// Mock database code moved to db-mock.ts for reference
// The application now uses Supabase for all database operations
