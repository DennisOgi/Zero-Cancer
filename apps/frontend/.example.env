VITE_ENV_MODE=development
VITE_DEV_API_BASE_URL=http://localhost:8787

# Optional: only needed for direct browser uploads (center result uploads).
# Patient registration photos upload through the backend API instead.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
