{
  "name": "zerocancer-frontend",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "cd apps/frontend && npm install && npm run build",
    "dev": "cd apps/frontend && npm run dev",
    "start": "cd apps/frontend && npm run preview"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}